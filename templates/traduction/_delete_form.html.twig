<form method="post" action="{{ path('app_traduction_delete', {'id': traduction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traduction.id) }}">
    <button class="btn">Delete</button>
</form>
