{% extends 'base.html.twig' %}

{% block title %}Traduction{% endblock %}

{% block body %}
    <h1>Traduction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ traduction.id }}</td>
            </tr>
            <tr>
                <th>Labeltrad</th>
                <td>{{ traduction.labeltrad }}</td>
            </tr>
            <tr>
                <th>Descriptiontrad</th>
                <td>{{ traduction.descriptiontrad }}</td>
            </tr>
            <tr>
                <th>Lang</th>
                <td>{{ traduction.lang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_traduction_index') }}">back to list</a>

    <a href="{{ path('app_traduction_edit', {'id': traduction.id}) }}">edit</a>

    {{ include('traduction/_delete_form.html.twig') }}
{% endblock %}
